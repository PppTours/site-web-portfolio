{
  "name": "polybook",
  "version": "0.0.0",
  "workspaces": [
    "front",
    "api"
  ],
  "scripts": {
    "prepare": "husky install",
    "start:front": "pnpm --filter front run start",
    "start:api": "pnpm --filter api run start:dev"
  },
  "devDependencies": {
    "@commitlint/cli": "18.6.0",
    "@commitlint/config-conventional": "18.6.0",
    "husky": "8.0.0"
  }
}
